<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="token">

    <resultMap id="TokenMap" type="Token">
        <result property="clientId" column="client_id" javaType="java.lang.String" jdbcType="VARCHAR"/>
        <collection property="scopes" column="{clientId=client_id,userCode=user_id}" javaType="java.util.ArrayList" ofType="String" select="token.selectScopeList"/>
    </resultMap>

    <insert id="insertToken" parameterType="Token">
        INSERT INTO Token
        (token_id,
        user_id,
        client_id,
        create_time,
        expire_date)
        VALUES
        (#{tokenId},
        #{userCode},
        #{clientId},
        #{createTime},
        #{expireDate})
    </insert>

    <select id="getToken" parameterType="String" resultMap="TokenMap">
        select token_id,
			   user_id,
			   client_id,
			   create_time,
			   expire_date
          from Token
         where token_id = #{tokenId}
    </select>

    <select id="selectScopeList" parameterType="java.util.Map" resultType="String">
        select scope_id
        from UserClientScope
        where client_id = #{clientId}
          AND user_id = #{userCode}
    </select>



    <select id="findToken" resultType="Token" parameterType="AuthenticationRequest">
        select token_id,
			   user_id,
			   client_id,
			   create_time,
			   expire_date
          from Token
         where user_id = #{userCode}
           and client_id = #{clientId}
    </select>

    <delete id="deleteToken" parameterType="String">
        DELETE FROM Token
         WHERE token_id = #{tokenId}
    </delete>

    <delete id="deleteTokenByUser" parameterType="AuthenticationRequest">
      DELETE FROM Token
         WHERE user_id = #{userCode}
    </delete>

    <delete id="deleteTokenByClient" parameterType="String">
        DELETE FROM Token
        WHERE client_id = #{param1}
    </delete>

    <select id="selectTokens" resultType="Token">
        select token_id,
			   user_id,
			   client_id,
			   create_time,
			   expire_date
          from Token
    </select>

    <select id="findByAdminToken" resultType="Token">
        select token_id,
                user_id,
                client_id,
                create_time,
                expire_date
         from Token
        where token_id = #{tokenId}
          and exists  (select 1
                         from UserClientScope
                        where (user_id, scope_id) = (select user_id
                                                          , 'Admin'
                                                       from Token
                                                      where token_id = #{tokenId}))
    </select>
</mapper>
